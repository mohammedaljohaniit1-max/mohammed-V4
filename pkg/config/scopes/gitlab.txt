# ═══════════════════════════════════════════════════════════════════════════
# MOHAMMED V12.3 RUTHLESS · Built-in scope: GitLab (HackerOne public program)
# ---------------------------------------------------------------------------
# Invoke with:  ./mohammed scan --scope gitlab -c config.yaml --profile large
#
# Lines prefixed with '!' (or '-') are OUT OF SCOPE and are parsed as EXCLUDES
# — they are NEVER enumerated, probed, or reported. This is the exact set of
# excludes that caused the 8-hour Phase 12 collapse when previously treated as
# targets (!*.service-now.com, !*.gitlab.cn, …).
#
# FAILURE 8 — this list is the EXACT HackerOne GitLab program scope, not the
# prior over-broad guess (gitlab.company / gitlabstatus.com were NOT in scope).
#
# Source: HackerOne GitLab program scope (in-scope apex/host + out-of-scope).
# ═══════════════════════════════════════════════════════════════════════════

# ── IN SCOPE ────────────────────────────────────────────────────────────────
gitlab.com
registry.gitlab.com
customers.gitlab.com
docs.gitlab.com
design.gitlab.com
advisories.gitlab.com
about.gitlab.com
*.gitlap.com
*.gitlab.org
*.gitlab.net

# ── OUT OF SCOPE (excluded — never enumerated) ───────────────────────────────
!us-federal-gitlab.com
!translate.gitlab.com
!support.gitlab.com
!status.gitlab.com
!shop.gitlab.com
!partners.gitlab.com
!packages.gitlab.com
!levelup.gitlab.com
!ir.gitlab.com
!gitlabtraining.cloud
!gitlabsandbox.net
!gitlabdemo.cloud
!gitlab.biterg.io
!forum.gitlab.com
!federal-support.gitlab.com
!dashboards.gitlab.com
!aptly.gitlab.com
!alerts.gitlab.com
!*.service-now.com
!*.runway.gitlab.net
!*.gitlab.cn
!gitlab-private.org
!*.gitlab-private.org
