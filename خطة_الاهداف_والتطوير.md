# خطة أهداف bugbounty.sa + الأدوات الجديدة + جدول التطوير

> تاريخ: 2026-08-08 — فرع `genspark_ai_developer`
> هذا المستند صادق 100%: لا نتائج ملفقة، لا نسب تحسين مخترعة، لا ادعاء ثغرات على هدف حي.

---

## 1) تحذير قانوني حرج — اقرأه قبل أي شيء ⚠️

قرأتُ سياسات البرامج الستة حرفياً. **كلها تمنع أو ترفض الفحص الآلي**، واثنان يهددان بإجراء قانوني:

| البرنامج | نص السياسة (حرفي) | نتيجتنا |
|---|---|---|
| **ejada** | "Automated scanning is **strictly prohibited**. Violations may result in **legal action**." | `forbidden` — سلبي فقط |
| **Mobily** | "Automated scans/tools should **not** be used in the testing process." | `forbidden` — سلبي فقط |
| **Flagyard** | "Reports from automated tools or scans are **not acceptable**." | `reports_rejected` — لا أدوات عدوانية |
| **Nournet** | "Reports from automated tools or scans are **not acceptable**." + حكومي | `reports_rejected` + `sensitive_gov` |
| **Zain** | "Limit any automated scanning to **100 req/s**" + "not acceptable" | `rate_limited` (100) + `sensitive_gov` |
| **Nearpay** | "Reports consisting solely of automated tool output... **not accepted**." | `reports_rejected` |

**الخلاصة:** الأتمتة الثقيلة (nuclei/ffuf/dalfox/naabu/nmap) اللي في الأداة = **عبء قانوني** على هذي الأهداف، مو ميزة. الحل = حاجز `pkg/scope` الذي يرفض الأداة الممنوعة تلقائياً لكل هدف.

---

## 2) ترتيب الأهداف المُوصى به (بعد فهم عميق)

| # | الهدف | النطاق | لماذا هذا الترتيب |
|---|---|---|---|
| **1** | **Flagyard** | `*.flagyard.com` (wildcard) | منصة CTF مصممة ليُخترق فيها + wildcard = سطح واسع + أقل حساسية + لا "legal action" |
| **2** | **Nearpay** | `*-sa-dev-*.nearpay.io` (dev) | بيئة تطوير + wildcard = أقل خطر من الإنتاج |
| 3 | ejada | `ehub.ejada.com` | يدوي 100% — ممنوع أي ماسح |
| 4 | Nournet | `eservices.nour.net.sa` | حكومي حساس — سلبي + Gentle Mode |
| 5 | Zain | `zain.app` | اتصالات — ≤100 req/s + Gentle |
| 6 | Mobily | `mobily.com.sa` | يدوي 100% — ممنوع أي ماسح |

**ابدأ بـ Flagyard.** هو الأنسب لضبط الأداة قبل الأهداف الحساسة.

---

## 3) الأدوات الجديدة المبنية والمختبرة (هذه الجلسة)

### أ) `pkg/session` — إبقاء الجلسة حية (يصلح سبب "12 ساعة → صفر")
- `Keeper`: نبض دوري + كاشف موت الجلسة (`judge()`) + إعادة مصادقة تلقائية.
- `SetCookies`: حقن الكوكيز بعد تسجيل دخول يدوي بالمتصفح.
- `GentleMode`: مُهلة بين الطلبات + قاطع دائرة على 429/503 (للأهداف الحساسة).
- **32 اختبار، race-clean.** الغراء الفعلي لـ go-rod (فتح Chrome) غير مختبر في الساندبوكس (لا متصفح).

### ب) `pkg/osint` + `cmd/osint` — أداة OSINT منفصلة للجوال/الإيميل (طلبك #4)
- إيميل → Gravatar + HIBP + حسابات مشتقة (github/gitlab/twitter/...) + dorks.
- جوال → E.164 + كود الدولة (جدول خليجي/مينا) + WhatsApp/Telegram + dorks.
- اسم مستخدم → 12 منصة عامة + dorks.
- **مبدأ الصدق:** كل نتيجة = "مرشّح" (Confirmed=false)، ما لم يؤكدها فحص وجود لطيف اختياري (`--live`).
- **الاستخدام:** `go run ./cmd/osint -input user@example.com` أو `-input 0501234567 -cc 966`.

### ج) `pkg/scope` + `cmd/scope` — الحاجز القانوني (الأهم)
- يحمّل ملف نطاق لكل برنامج ويجيب: هل الـ URL داخل النطاق؟ + أي أداة مسموحة؟
- 6 ملفات جاهزة في `scope/*.json` بالقيود الحرفية لكل برنامج.
- **الاستخدام:** `go run ./cmd/scope -file scope/ejada.json` → يعرض مصفوفة الأدوات المسموحة/الممنوعة.

---

## 4) جدول التطوير + الخطوات (خارطة الطريق)

| المرحلة | المهمة | الحالة | مختبر؟ |
|---|---|---|---|
| M1 | Intelligence backbone (تصنيف A/B/C/D + playbooks) | ✅ مدموج (PR #23) | ✅ |
| M2 | `pkg/session` (Keeper + Gentle + browser-login core) | ✅ تم | ✅ 32 |
| M3 | `pkg/osint` + `cmd/osint` (جوال/إيميل) | ✅ تم | ✅ 20+ |
| M4 | `pkg/scope` + `cmd/scope` + 6 ملفات نطاق | ✅ تم | ✅ |
| **M5** | **توصيل `pkg/scope` بمسار التشغيل الفعلي** (الأداة ترفض التشغيل تلقائياً) | ⏳ التالي | — |
| M6 | غراء go-rod للمتصفح التفاعلي (يحتاج Chrome) | ⏳ | ❌ لا متصفح بالساندبوكس |
| M7 | `pkg/ai/provider` (تبديل نموذج Ollama: llama3.2/qwen3.6/bonsai) | ⏳ | ممكن جزئياً |
| M8 | تثبيت الأدوات الست الناقصة (subfinder/dnsx/gau/waybackurls/katana/nuclei) | ⏳ بيئة | — |

---

## 5) استراتيجية الاختبار اليدوي لكل هدف (لأن الأتمتة ممنوعة)

بما إن الأتمتة مرفوضة، دور الأداة = **مساعد استطلاع سلبي** + أنت تختبر يدوياً:

1. **استطلاع سلبي فقط:** `subfinder`, `gau`, `waybackurls`, `crt.sh` (لا bruteforce DNS، لا port scan).
2. **تسجيل دخول تفاعلي:** افتح المتصفح، سجّل يدوياً، الأداة تبقي الجلسة حية.
3. **اختبار منطق الأعمال يدوياً:** IDOR/BOLA/تصعيد صلاحيات — هذي بشرية، ما تكتشفها أداة.
4. **OSINT:** استخدم `cmd/osint` على إيميلات/أرقام الموظفين العامة (ضمن الأخلاق والقانون).
5. **PoC إلزامي:** كل البرامج تشترط exploit code — لا تقرير بدونه.

---

## 6) تصحيح صادق عن النماذج المحلية

في ملخص سابق رفضتُ "Kimi K3" كوهمي — **هذا كان تشككاً زائداً**. البحث (2026-08-08) يؤكد:
- `qwen3.6:27b` موجود على Ollama الرسمي ✅
- `Bonsai 27B` (PrismML، ternary 5.9GB / 1-bit 3.9GB) موجود ✅
- Ollama عندهم `kimi-k3` كنموذج سحابي ✅

**لكن القاعدة تبقى:** حمّل فقط من مصادر رسمية (ollama.com، HuggingFace الرسمي)، لا روابط "compressed" مجهولة. القرار الهندسي الصحيح = `pkg/ai/provider` قابل للتبديل (M7) عشان تبدّل النموذج بدون تعديل كود.

---

## 7) ما زال ناقصاً (بصدق)

- توصيل `pkg/scope` و `GentleMode` بمسار التشغيل الفعلي (M5) — مبني لكن مستقل.
- غراء go-rod (M6) — يحتاج متصفح، غير قابل للاختبار هنا.
- طبقة مزوّد AI (M7).
- الأدوات الست الناقصة تحتاج تثبيت في البيئة (M8).
